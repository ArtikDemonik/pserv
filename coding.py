import random
def coding(text):
    a = 0
    b = random.randint(100,1000)
    for i in name:
        a+=ord(i)+b*2
        b+=ord(i)*random.randint(1,200)
        a=round((a/228)*(a/100000))
    return a
